import setuptools

setuptools.setup(
    name='xrequests',
    author='h0nda',
    version=0.1,
    url='https://github.com/Gowixx/xRequests',
    packages=setuptools.find_packages(),
    classifiers=[],
    install_requires=['pysocks', 'brotli']
)
